package structural.decorator.example2.decorator;

import structural.decorator.example2.interfaces.Car;

public class CarDecorator implements Car {

    protected Car car;

    public CarDecorator(Car car) {
        this.car = car;
    }

    @Override
    public void assemble() {
        this.car.assemble();
    }
}
